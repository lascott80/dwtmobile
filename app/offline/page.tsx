export default function OfflinePage() {
  return (
    <main className="offline-shell">
      <section className="offline-card">
        <p>Offline</p>
        <h1>You&apos;re between signals.</h1>
        <span>Your last saved park data is still available from the home screen when cached.</span>
      </section>
    </main>
  );
}
